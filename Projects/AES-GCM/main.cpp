#include "header.h"
#include <QApplication>

int main(int argc, char *argv[]) {
    /*QApplication app(argc, argv);
    MainGUI *mainGUI = nullptr;
    UserInput userInput = GetUserInput(argc, argv, &mainGUI);

    if (!userInput.valid || !mainGUI) {
        printf("ERROR: Input is incomplete or GUI was closed\n");
        return 1;
    }

    pnum = GetProcNum();

    mainGUI->startProcessing();

    int res = app.exec();

    return res;*/

    return 0;
}